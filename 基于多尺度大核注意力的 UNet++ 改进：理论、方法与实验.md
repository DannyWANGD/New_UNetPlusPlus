# 基于多尺度大核注意力的 UNet++ 改进：理论、方法与实验

> 本文档详细阐述了如何将 MAN 论文（2209.14145）中的多尺度大核注意力（MLKA）和门控空间注意力单元（GSAU）引入 UNet++ 架构，用于遥感语义分割任务。内容涵盖 UNet++ 与 MAN 的核心原理、迁移可行性分析、具体改进方案、三阶段实验设计及创新点总结，所有公式均使用 `$$` 包裹，便于组会汇报与论文写作。

---

## 1. 引言：遥感分割与 UNet++ 剪枝的机遇与挑战

遥感影像语义分割需要对地物进行像素级分类，既要求模型能捕捉大范围上下文（如整片农田、大型建筑群），又需保留精细边界（如道路、独立树木）。UNet++ 作为一种经典的医学图像分割模型，通过嵌套密集跳跃连接在多个数据集上取得了优异性能，并因其深度监督特性支持模型剪枝，可在推理速度与精度间灵活权衡。

我们此前已成功复现 UNet++ 的深度监督剪枝实验，在细胞核数据集上验证了 **L1 剪枝（仅使用最浅层输出）相比完整 L4 模型可获得 4 倍推理加速**。然而，剪枝后的 L1 输出存在明显的边缘粗糙、密集区域粘连问题，这是因为浅层特征缺乏足够的上下文信息与多尺度融合能力。这一现象在遥感影像中更为突出：地物尺度变化剧烈，单纯依赖局部卷积难以同时捕捉微小道路与大型地物的结构。

因此，我们希望在**最小改动 UNet++ 架构**的前提下，引入轻量级注意力机制，增强模型的多尺度感知与长程依赖建模能力，同时保持剪枝优势，最终实现遥感影像的“快速筛查 + 精细分割”分级推理框架。

---

## 2. UNet++：结构、特性与剪枝基础

### 2.1 UNet++ 的整体结构

UNet++ 是一种编码器-解码器网络，其核心创新在于**嵌套的密集跳跃连接**。设编码器第 $$i$$ 层（下采样层）的输出为 $$x^{i,0}$$，解码器路径中通过上采样逐步恢复分辨率。在编码器与解码器之间，UNet++ 引入了多个中间节点，记为 $$x^{i,j}$$，其中 $$j$$ 表示跳跃路径中的“深度”索引（$$j = 0$$ 为编码器输出，$$j > 0$$ 为经过若干卷积后的特征）。

**节点计算公式**（UNet++ 论文 Eq. 1）：

$$
x^{i,j} = \mathcal{H}\left( \big[ [x^{i,k}]_{k=0}^{j-1},\; \mathcal{U}(x^{i+1,j-1}) \big] \right)
$$

其中：
- $$[\cdot]$$ 表示特征拼接（concatenation）；
- $$\mathcal{U}(\cdot)$$ 为上采样（通常为双线性插值或转置卷积）；
- $$\mathcal{H}(\cdot)$$ 为卷积单元，原始实现为 **3×3 卷积 + 批归一化 + ReLU**。

该公式的含义：节点 $$x^{i,j}$$ 融合了**同一跳跃路径上前 $$j$$ 个节点**的输出（即 $$x^{i,0}, x^{i,1}, \dots, x^{i,j-1}$$）以及**下一层跳跃路径中上一个节点**上采样后的特征（$$\mathcal{U}(x^{i+1,j-1})$$），然后通过 $$\mathcal{H}$$ 进行非线性变换。这种设计使得特征在传递过程中逐步被“提纯”，减小了编码器浅层特征与解码器深层特征的语义差距。

### 2.2 深度监督与剪枝

UNet++ 在四个不同语义层级的输出（$$x^{0,1}, x^{0,2}, x^{0,3}, x^{0,4}$$）上分别附加分割头（1×1 卷积 + sigmoid），并在训练时对各输出同时计算损失（深度监督）。训练完成后，推理时可以根据需要选择任意一个输出作为最终分割结果：

- **L4（完整模型）**：使用 $$x^{0,4}$$，精度最高，速度最慢。
- **L1（剪枝版本）**：使用 $$x^{0,1}$$，仅经过一次跳跃路径融合，速度快但精度较低。
- 中间等级（L2, L3）在速度与精度间提供折衷。

我们之前的实验表明，在细胞核数据集上 L1 剪枝相比 L4 可获得 **4 倍加速**（1866 ms → 466 ms），但 L1 输出的边缘质量明显下降（见图 1 的对比）。

---

## 3. MAN 中的轻量注意力机制：LKA、MLKA 与 GSAU

MAN（Multi-scale Attention Network）是为单图像超分辨率设计的卷积网络，其目标是让卷积网络在不引入 Transformer 巨大计算量的前提下，获得大感受野与多尺度注意力。核心模块包括 LKA、MLKA 和 GSAU。

### 3.1 大核注意力（Large Kernel Attention, LKA）

MAN 借鉴了视觉注意力网络（VAN）的思想，将一个大核卷积分解为三个连续操作，以极低的参数增量实现大感受野。设输入特征为 $$X \in \mathbb{R}^{C \times H \times W}$$，LKA 定义为：

$$
\text{LKA}(X) = f_{\text{PW}}\big( f_{\text{DW-dil}}( f_{\text{DW}}(X) ) \big)
$$

- $$f_{\text{DW}}$$：深度卷积，核大小为 $$(2d-1) \times (2d-1)$$，捕捉局部细节；
- $$f_{\text{DW-dil}}$$：空洞深度卷积，核大小为 $$\lceil K/d \rceil \times \lceil K/d \rceil$$，空洞率为 $$d$$，用于扩大感受野；
- $$f_{\text{PW}}$$：逐点卷积（1×1 卷积），混合通道信息。

**作用**：通过分解，等效于一个 $$K \times K$$ 的大核卷积，但参数量仅为 $$\mathcal{O}(C)$$ 而非 $$\mathcal{O}(C^2)$$，同时保持了空间上对远距离像素的响应能力。

### 3.2 多尺度大核注意力（Multi-scale Large Kernel Attention, MLKA）

单纯使用固定参数的 LKA 无法适应不同尺度的结构。MLKA 在 LKA 基础上引入多尺度与门控机制：

1. **通道分组**：将输入 $$X$$ 在通道维度上分成 $$n$$ 组，每组 $$X_i$$ 尺寸为 $$\frac{C}{n} \times H \times W$$。
2. **多尺度 LKA**：对第 $$i$$ 组使用不同参数的 LKA（例如 $$\{(7,2), (21,3), (35,4)\}$$），分别捕捉局部、中距和全局依赖。
3. **门控聚合**：为每组额外生成一个空间门控 $$G_i(X_i)$$，用于动态调整注意力图：

$$
\text{MLKA}_i(X_i) = G_i(X_i) \otimes \text{LKA}_i(X_i)
$$

其中 $$G_i$$ 由深度卷积（核大小与 $$a_i$$ 相同）生成并经过 sigmoid 激活，$$\otimes$$ 为逐元素乘法。

**门控的作用**：MAN 的作者指出，大核 LKA 中的空洞卷积会产生“棋盘格伪影”（blocking artifacts），如图 4 所示。门控机制通过引入可学习的空间权重，能有效抑制这些伪影，使注意力图更平滑。

**MLKA 的总计算量**：由于通道分组，每组计算复杂度降低，总体仍保持轻量。

### 3.3 门控空间注意力单元（Gated Spatial Attention Unit, GSAU）

MAN 提出 GSAU 作为前馈网络的替代，用于增强空间特征。给定两个同尺寸特征 $$X, Y \in \mathbb{R}^{C \times H \times W}$$，GSAU 定义为：

$$
\text{GSAU}(X,Y) = f_{\text{DW}}(X) \otimes Y
$$

- $$f_{\text{DW}}$$ 是一个深度卷积（如 7×7），生成空间门控。
- 该门控对 $$Y$$ 进行加权，突出重要空间位置。

**优势**：相比传统 MLP（参数多、计算量大），GSAU 以极小的计算代价实现了空间自适应加权，且不引入额外非线性，适合嵌入解码器恢复细节。

---

## 4. 为什么 UNet++ 的特性适合迁移 MLKA 和 GSAU？

UNet++ 具备以下三个关键特性，使得 MAN 的轻量注意力模块能自然地嵌入并发挥效果：

### 4.1 多层级特征融合

UNet++ 的每个跳跃节点 $$x^{i,j}$$ 都扮演着**多源特征融合**的角色：它需要将来自编码器不同深度的特征（$$x^{i,0}, x^{i,1}, \dots$$）与解码器传递来的上采样特征（$$\mathcal{U}(x^{i+1,j-1})$$）进行综合。这种融合场景恰恰需要多尺度感受野：编码器浅层特征富含局部纹理，深层特征蕴含语义上下文，而解码器上采样特征则需要空间细节。MLKA 的多尺度分组与门控机制能够在此处同时提取局部、中距、全局信息，并通过门控自适应调整融合权重，从而获得更优的中间表示。

### 4.2 深度监督与剪枝

UNet++ 的多个输出头（$$x^{0,1}$$ 至 $$x^{0,4}$$）对应不同深度的融合结果。浅层输出（L1）只经过少量卷积，若能在这些早期节点中引入 MLKA，即可让浅层特征也具备一定的多尺度上下文，从而显著提升剪枝后的精度。GSAU 则可加在输出头之前，进一步强化空间细节。

### 4.3 密集连接与梯度流动

UNet++ 的嵌套密集连接使得每个节点都接收来自同层前序节点的输入，这种结构与 DenseNet 类似，有助于梯度传播。将 MLKA 作为 $$\mathcal{H}$$ 替换后，仍保持密集连接形式，不会破坏原有信息流。同时，MLKA 中的门控机制可视为一种可学习的特征重标定，与密集连接的累积效果相辅相成。

---

## 5. 原有问题与改进的理论依据

### 5.1 问题一：固定感受野与尺度单一

**原始问题**：UNet++ 的 $$\mathcal{H}$$ 为 3×3 卷积，感受野有限，无法同时捕捉局部细节与全局结构。对于遥感影像中尺度差异极大的地物（如大型水库与小型车辆），单一尺度的卷积难以兼顾。

**改进方案**：将 $$\mathcal{H}$$ 替换为 MLKA。MLKA 通过多组不同大小的 LKA 并行处理，每组对应不同感受野，相当于在融合过程中隐式地对不同尺度的特征进行加权。门控机制进一步抑制了空洞卷积带来的伪影，使融合结果更干净。

**理论依据**：设节点输入为 $$Z = \big[ [x^{i,k}]_{k=0}^{j-1},\; \mathcal{U}(x^{i+1,j-1}) \big]$$，原输出为 $$\mathcal{H}(Z) = \text{ReLU}(\text{BN}(\text{Conv}_{3\times3}(Z)))$$。替换后：

$$
\mathcal{H}_{\text{new}}(Z) = \text{MLKA}(Z) = \bigoplus_{i=1}^n \left( G_i(Z_i) \otimes \text{LKA}_i(Z_i) \right)
$$

其中 $$\bigoplus$$ 表示通道拼接。由于 LKA 的大感受野，MLKA 能聚合更广范围的信息；而多尺度分组使得模型对不同尺度的结构都有响应，从而改善尺度适应性。

### 5.2 问题二：剪枝后浅层输出质量低

**原始问题**：L1 输出仅经过 $$x^{0,1}$$，其输入来自编码器浅层和少量融合，缺乏全局上下文，导致边缘粗糙、粘连。

**改进方案**：在 $$x^{0,1}$$ 的计算中，原本的 $$\mathcal{H}$$ 被替换为 MLKA。由于 MLKA 的多尺度特性，即便在早期节点，也能通过大核分组捕捉到一定的全局信息，提升分割结果的连续性。此外，我们在深度监督输出前（如 $$x^{0,1}$$ 之后）增加轻量 GSAU，可进一步强化空间边缘。

**理论依据**：剪枝后的输出 $$x^{0,1}$$ 在原始 UNet++ 中仅由 $$x^{0,0}$$（编码器第一层）和 $$\mathcal{U}(x^{1,0})$$ 拼接后经一次卷积得到，感受野受限。引入 MLKA 后，即便只有一层融合，也能通过多尺度大核获取更广的上下文，从而减少粘连并细化边缘。

### 5.3 问题三：深度监督未充分利用

**原始问题**：深度监督的各输出头独立训练，但各层级之间缺乏交互，未能将深层语义信息有效传递给浅层。

**改进方案**：在所有深度监督分支前（即 $$x^{0,1}, x^{0,2}, x^{0,3}, x^{0,4}$$ 之后）均添加轻量 GSAU，让每个输出头在最终预测前都经过空间加权。这相当于在多个层级上强制模型关注空间显著区域，使得不同层级的监督信号更加一致，有助于梯度回传。

**理论依据**：GSAU 的门控 $$f_{\text{DW}}(x^{0,j})$$ 可以视作对当前特征图的空间重要性重标定，其梯度会通过深度监督的损失函数反向传播到所有前置节点，从而让浅层节点也能获得来自深层监督的空间注意力指导。

---

## 6. 具体改进方案：最小改动嵌入 MLKA 与 GSAU

保持 UNet++ 整体架构不变，仅修改三处关键位置：

### 6.1 改动一：将跳跃路径中的 $$\mathcal{H}$$ 替换为 MLKA

- **位置**：每个节点 $$x^{i,j}$$ 中，将原始卷积单元 $$\mathcal{H}$$ 替换为 MLKA。
- **配置**：轻量化设计，通道分组数 $$n = 2$$ 或 $$3$$，核配置可选用 $$\{(3,5,1), (5,7,1)\}$$（两尺度）或增加 $$(7,9,1)$$（三尺度）。门控使用对应的小核深度卷积。
- **输入输出维度**：输入为拼接后的特征，输出通道数保持与原设计一致（通常为 $$C_{\text{out}}$$）。MLKA 内部采用 1×1 卷积进行最终通道映射，保证维度匹配。

### 6.2 改动二：解码器上采样后添加 GSAU

- **位置**：在每个解码器上采样操作（即 $$\mathcal{U}(x^{i+1,j-1})$$）之后，将上采样特征与对应跳跃路径特征通过 GSAU 融合，替代原有的简单卷积或残差块。
- **公式**：设上采样特征为 $$X_{\text{up}}$$，跳跃路径特征为 $$X_{\text{skip}}$$（通常来自 $$x^{i,j-1}$$ 或其他），则新输出为：

$$
\text{Output} = \text{GSAU}(X_{\text{up}}, X_{\text{skip}}) = f_{\text{DW}}(X_{\text{up}}) \otimes X_{\text{skip}}
$$

- **效果**：GSAU 的空间门控可使解码器更关注边缘区域，提升分割细节。

### 6.3 改动三：深度监督分支前添加轻量注意力

- **位置**：在每个深度监督输出头（$$x^{0,1}, x^{0,2}, x^{0,3}, x^{0,4}$$）之后、1×1 分类卷积之前，插入一个轻量 GSAU 或单尺度 MLKA。
- **目的**：增强各层级输出在最终预测前的空间分辨能力，使深度监督更有效。

### 6.4 附加改进：自适应类别感知加权损失（ACWL）

用户已设计的 ACWL 损失可无缝集成到训练中，用于处理遥感地物类别不均衡问题，进一步提升小目标与边缘的识别能力。

---

## 7. 三阶段实验设计

### 7.1 第一阶段：改进验证（主实验）

- **数据集**：遥感语义分割公开数据集（如 DeepGlobe、LandCover.ai 或自建数据集）。
- **对比模型**：
  - **Baseline**：原始 UNet++（深度监督，超参调优）。
  - **+MLKA**：仅替换跳跃路径 $$\mathcal{H}$$ 为 MLKA（保持其他不变）。
  - **+GSAU**：仅在解码器添加 GSAU（不替换跳跃路径）。
  - **Full**：MLKA + GSAU + 深度监督前 GSAU（完整改进）。
- **评估指标**：平均 IoU、边界 IoU（评估边缘质量）、参数量、FLOPs、推理时间（ms）。
- **预期结果**：Full 模型在 IoU 上显著高于 Baseline，边界 IoU 提升明显；MLKA 和 GSAU 各自贡献正向效果。

### 7.2 第二阶段：剪枝量化

- **训练**：对 Full 模型进行完整训练（深度监督）。
- **剪枝测试**：分别取 $$x^{0,1}, x^{0,2}, x^{0,3}, x^{0,4}$$ 作为输出，测量：
  - 推理时间（ms）
  - 加速比（相对于 L4）
  - 分割精度（IoU、边界 IoU）
- **对比**：与原始 UNet++ 的剪枝结果对比，重点观察 L1 的精度提升。
- **预期**：Full 模型的 L1 **精度**相比原 L1 有明显提高（例如 IoU 提升 5~10 个百分点），而**加速比**仍维持在 4 倍左右。

### 7.3 第三阶段：分级推理模拟

- **场景**：模拟遥感影像处理，整景影像输入。
- **策略**：
  - **快速筛查**：用 L1 模型对全图进行推理，得到粗分割结果。
  - **区域选择**：识别出置信度较低或面积较大的区域（如建筑群、农田边界）。
  - **精细分割**：仅对这些区域用 L3 或 L4 模型进行二次推理，并拼接结果。
- **评估**：总推理时间（对比全图用 L4 推理）、最终分割精度（加权 IoU）。
- **预期**：在总时间减少 50% 以上的情况下，最终精度接近 L4 单模型。

---

## 8. 创新点总结

1. **首次将多尺度大核注意力（MLKA）引入 UNet++ 的嵌套跳跃路径**  
   通过替换节点中的普通卷积为 MLKA，使特征融合过程具备多尺度感受野，同时利用门控机制抑制空洞卷积伪影，显著增强模型对尺度变化地物的适应性。

2. **设计轻量门控空间注意力单元（GSAU）嵌入解码器与深度监督分支**  
   用极简的空间门控模块替代冗余卷积，在降低参数量的同时强化边缘恢复能力，并让多层级监督共享空间注意力信息。

3. **提出面向遥感影像的深度监督分级推理框架**  
   结合 UNet++ 的剪枝特性，实现“快速筛查 + 精细分割”两阶段推理，在保持高精度的前提下获得 4 倍推理加速，为低-高算力协同处理提供实用方案。

4. **综合实验验证与理论分析**  
   从消融实验、剪枝量化到分级推理模拟，系统证明改进方法的有效性，并给出理论解释，为后续研究提供可复现的基准。

---

## 9. 附录：关键公式与代码片段

### 9.1 MLKA 简化版 PyTorch 实现

```python
class MLKA(nn.Module):
    def __init__(self, in_channels, out_channels, kernel_configs=[(3,5,1), (5,7,1), (7,9,1)]):
        super().__init__()
        self.num_groups = len(kernel_configs)
        self.group_channels = in_channels // self.num_groups
        self.groups = nn.ModuleList()
        self.gates = nn.ModuleList()

        for a, b, d in kernel_configs:
            # LKA 部分
            self.groups.append(nn.Sequential(
                nn.Conv2d(self.group_channels, self.group_channels, a, padding=a//2, groups=self.group_channels),
                nn.Conv2d(self.group_channels, self.group_channels, b, padding=b//2*d, dilation=d, groups=self.group_channels),
                nn.Conv2d(self.group_channels, self.group_channels, 1)
            ))
            # 门控
            self.gates.append(
                nn.Conv2d(self.group_channels, self.group_channels, a, padding=a//2, groups=self.group_channels)
            )
        self.fusion = nn.Conv2d(in_channels, out_channels, 1)

    def forward(self, x):
        splits = torch.split(x, self.group_channels, dim=1)
        outputs = []
        for x_group, lka, gate in zip(splits, self.groups, self.gates):
            attn = lka(x_group)
            g = torch.sigmoid(gate(x_group))
            outputs.append(g * attn)
        out = torch.cat(outputs, dim=1)
        out = self.fusion(out)
        return out
```

### 9.2 GSAU 实现

```python
class GSAU(nn.Module):
    def __init__(self, channels, kernel_size=7):
        super().__init__()
        self.dw_conv = nn.Conv2d(channels, channels, kernel_size, padding=kernel_size//2, groups=channels)

    def forward(self, x, y):
        gate = torch.sigmoid(self.dw_conv(x))
        return gate * y
```

### 9.3 关键符号表

| 符号                  | 含义                                                     |
| --------------------- | -------------------------------------------------------- |
| $$C$$                 | 通道数（Channel）                                        |
| $$H, W$$              | 特征图高度、宽度                                         |
| $$X$$                 | 输入特征图，形状 $$B \times C \times H \times W$$        |
| $$f_{\text{DW}}$$     | 深度卷积（Depth-wise Convolution）                       |
| $$f_{\text{DW-dil}}$$ | 空洞深度卷积（Dilated Depth-wise Convolution）           |
| $$f_{\text{PW}}$$     | 逐点卷积（Point-wise Convolution，1×1）                  |
| $$\otimes$$           | 逐元素相乘                                               |
| $$\mathcal{U}$$       | 上采样（Upsampling）                                     |
| $$[\cdot]$$           | 特征拼接（Concatenation）                                |
| $$\mathcal{H}$$       | 卷积单元                                                 |
| $$x^{i,j}$$           | UNet++ 中第 $$i$$ 层（编码器深度）、第 $$j$$ 个节点      |
| $$a_i, b_i, d_i$$     | 第 $$i$$ 组 LKA 的深度卷积核大小、空洞卷积核大小、空洞率 |

---

## 10. 结语

本文档系统阐述了将 MAN 中的轻量注意力机制迁移到 UNet++ 的理论依据与具体实施方案。通过分析 UNet++ 的嵌套密集跳跃连接与深度监督特性，我们论证了 MLKA 和 GSAU 能够自然嵌入并解决原有模型在固定感受野、尺度适应性和剪枝精度下降方面的问题。三阶段实验设计将进一步量化改进效果，并为遥感影像的快速分级推理提供可靠技术支撑。该工作不仅拓展了 UNet++ 在遥感领域的应用潜力，也为未来结合 CNN 与 Transformer 优势的轻量化分割模型设计提供了新思路。

---

*文档最后更新：2026-03-27*